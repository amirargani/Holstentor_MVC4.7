@*@model  Restaurant.Models.Domin.Tbl_Nachricht*@
@model Restaurant.Models.ViewModels.MessageViewModel
@{
    ViewBag.Title = "Meine Verwaltung | Nachrichten antworten";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
    Restaurant.Models.Repository.HomeRep ivm = new Restaurant.Models.Repository.HomeRep();
    var qivm = ivm.GetIndexHeaderSelect();
}

@Html.Partial("P_HeaderBG")
<!-- Main Content -->
<main class="main">
    <!-- Section -->
    <section class="py-0">
        <div class="bg-web">
            <!-- Section -->
            <section class="py-0 pb-3">
                <div class="jumbotron jumbotron-fluid bg-rosa py-7">
                    <div class="container">
                        <h1 class="text-web fs-3 fs-lg-5 mt-5" data-aos="fade-down" data-aos-duration="1000">Meine Verwaltung</h1>
                        <h5 class="text-black mb-2" data-aos="fade-down" data-aos-duration="1100">Nachrichten antworten</h5>
                        <div class="tab-content py-3 px-3 px-sm-0">
                            <div class="tab-pane fade show active">
                                <!-- Pagination -->
                                <div class="col-12">
                                    <nav aria-label="Page navigation example">
                                        <ul class="pagination">
                                            <li><a class="btn btn-secondary mr-3" href="/Admin/Nachrichten"><i class="fas fa-chevron-circle-left mr-2"></i>Zurück</a></li>
                                        </ul>
                                    </nav>
                                </div>
                                <!-- End of Pagination -->
                                @*<form class="col px-5 px-lg-8 py-6 py-lg-0 mt-3" action="/Admin/NachrichtenAntwortenConfirmed" method="post">
                                    <input type="hidden" for="@Model.ID" name="id" value="@Model.ID" />
                                    <div class="form-group mb-4">
                                        <label><i class="fas fa-comment mr-2"></i>@Html.DisplayNameFor(model => model.Text):<br /><br /><span>@Html.DisplayFor(model => model.Text)</span></label>
                                    </div>
                                    <div class="form-group mb-4">
                                        @Html.TextAreaFor(model => model.TextAdmin, new { @class = "form-control", @rows = "8", @placeholder = "Nachricht" })
                                        @Html.ValidationMessageFor(model => model.TextAdmin, "", new { @class = "text-danger" })
                                    </div>
                                    <button type="submit" class="btn btn-lg btn-success btn-block mb-4 mt-4 text-capitalize">Antworten<i class="fas fa-reply ml-2"></i></button>
                                </form>*@
                                <form class="col px-5 px-lg-8 py-6 py-lg-0 mt-3" action="/Admin/NachrichtenAntwortenConfirmed" method="post">
                                    <input type="hidden" for="@ViewBag.ID" name="id" value="@ViewBag.ID" />
                                    <div class="form-group mb-4">
                                        <label><i class="fas fa-comment mr-2"></i>Text:<br /><span>@ViewBag.TextUser</span></label>
                                    </div>
                                    <div class="form-group mb-4">
                                        @Html.TextAreaFor(model => model.TextAdmin, new { @class = "form-control", @rows = "8", @placeholder = "Nachricht" })
                                        @Html.ValidationMessageFor(model => model.TextAdmin, "", new { @class = "text-danger" })
                                    </div>
                                    <button type="submit" class="btn btn-lg btn-success btn-block mb-4 mt-4 text-capitalize">Antworten<i class="fas fa-reply ml-2"></i></button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
                <h5 class="text-white mb-2 text-center" data-aos="fade-up" data-aos-duration="1600"><b>@qivm.NameSite</b></h5>
            </section>
            <!-- End of Section -->
        </div>
    </section>
</main>
<!-- End of Main Content -->
@Html.Partial("P_Footer")